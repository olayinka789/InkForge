{
  "name": "inkforge-blog",
  "version": "1.0.0",
  "description": "InkForge Blog - A full-stack blogging platform",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w backend & npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "start": "npm start -w backend"
  },
  "devDependencies": {
    "eslint": "^10.10.0"
  }
}
